## Resubmission

This is an update on vglmer 1.0.6 that adjusts one bug in how the model
summarized, adds some improvements for speed, and cleans up documentation.

It also addresses some warnings generated by the `Matrix` package.

## R CMD check results

There were no ERRORs, WARNINGs or NOTES. 

## Downstream dependencies

I have checked the downstream dependencies using
https://github.com/r-devel/recheck and all pass R CMD CHECK.